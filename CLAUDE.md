# Claude project entry point

Read and follow `AGENTS.md`, then read `v2/PROJECT_CONTEXT.md` before doing any work.

The canonical rebuild is `v2/` on `rebuild/snu-portals-supabase`. Root legacy code is reference-only. Update `v2/PROJECT_CONTEXT.md` in the same commit as every meaningful change so all models share the same project state.
