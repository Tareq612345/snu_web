import {createContext,useContext,useEffect,useMemo,useState} from 'react';
import {supabase} from '../lib/supabase';
const AuthContext=createContext(null);
export function AuthProvider({children}){const [session,setSession]=useState(null);const [profile,setProfile]=useState(null);const [loading,setLoading]=useState(true);useEffect(()=>{if(!supabase){setLoading(false);return;}supabase.auth.getSession().then(({data})=>load(data.session));const {data:{subscription}}=supabase.auth.onAuthStateChange((_event,next)=>load(next));return()=>subscription.unsubscribe();},[]);async function load(next){setSession(next);if(!next){setProfile(null);setLoading(false);return;}const {data}=await supabase.from('profiles').select('id,full_name,university_id,role,college_id,department_id').eq('id',next.user.id).maybeSingle();setProfile(data);setLoading(false);}const value=useMemo(()=>({session,profile,loading,signOut:()=>supabase?.auth.signOut()}),[session,profile,loading]);return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>;}export const useAuth=()=>useContext(AuthContext);
