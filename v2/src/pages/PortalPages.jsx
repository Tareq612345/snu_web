import {useEffect,useState} from 'react';import Icon from '../components/Icon';import {getSupabase} from '../lib/supabase';import {cleanText,isUuid,validateMaterial} from '../lib/validation';import {useAuth} from '../context/AuthContext';const Stat=({icon,label,value})=><article className="stat"><Icon name={icon}/><div><strong>{value}</strong><span>{label}</span></div></article>;export function StudentHome(){return <><div className="pageTitle"><div><span>مرحباً بك</span><h1>لوحة الطالب</h1><p>تابع موادك وآخر الملفات الأكاديمية.</p></div></div><div className="stats"><Stat icon="book" label="المقررات الحالية" value="—"/><Stat icon="file" label="ملفات جديدة" value="—"/><Stat icon="calendar" label="مهام قادمة" value="—"/><Stat icon="news" label="إعلانات" value="—"/></div><Courses/></>}export function FacultyHome(){return <><div className="pageTitle"><div><span>مساحة العمل الأكاديمية</span><h1>لوحة عضو هيئة التدريس</h1><p>إدارة المقررات ونشر المواد للطلاب.</p></div></div><div className="stats"><Stat icon="book" label="المقررات" value="—"/><Stat icon="users" label="الطلاب" value="—"/><Stat icon="file" label="المواد المنشورة" value="—"/><Stat icon="upload" label="مسودات" value="—"/></div><Courses faculty/></>}
export function Courses({faculty=false}){const {profile}=useAuth(),[rows,setRows]=useState([]),[loading,setLoading]=useState(true);useEffect(()=>{let live=true;(async()=>{const db=await getSupabase();if(!db||!profile){if(live)setLoading(false);return;}const relation=faculty?'course_staff!inner(profile_id)':'enrollments!inner(student_id)',field=faculty?'course_staff.profile_id':'enrollments.student_id';const {data}=await db.from('courses').select(`id,code,title,term,academic_year,${relation}`).eq(field,profile.id).eq('is_published',true).order('code').limit(24);if(live){setRows(data||[]);setLoading(false);}})();return()=>{live=false;};},[profile,faculty]);return <div className="contentBlock"><div className="blockHead"><div><h2>{faculty?'مقرراتي':'المقررات الحالية'}</h2><p>المحتوى الأكاديمي المتاح خلال الفصل الحالي</p></div></div>{loading?<p className="muted">جاري التحميل…</p>:rows.length?<div className="courseGrid">{rows.map(c=><article className="courseCard" key={c.id}><span>{c.code}</span><Icon name="book"/><h3>{c.title}</h3><p>{c.term} • {c.academic_year}</p><button>فتح المقرر</button></article>)}</div>:<div className="empty"><Icon name="book"/><h3>لا توجد مقررات مرتبطة بالحساب</h3><p>ستظهر المقررات هنا بعد تسجيلها من الإدارة.</p></div>}</div>}
export const Materials=()=> <EmptyPage title="المكتبة والملفات" text="الكتب والمذكرات والمحاضرات المنشورة في مقرراتك." icon="download"/>;export const Announcements=()=> <EmptyPage title="الإعلانات" text="التحديثات الرسمية الخاصة بمقرراتك والجامعة." icon="news"/>;export const Students=()=> <EmptyPage title="الطلاب" text="قوائم الطلاب المسجلين في مقرراتك." icon="users"/>;function EmptyPage({title,text,icon}){return <div className="contentBlock"><div className="blockHead"><div><h1>{title}</h1><p>{text}</p></div></div><div className="empty"><Icon name={icon}/><h3>لا توجد بيانات لعرضها الآن</h3></div></div>}
export function UploadMaterial(){const [file,setFile]=useState(null),[title,setTitle]=useState(''),[courseId,setCourseId]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);const {profile}=useAuth();async function submit(e){e.preventDefault();const fileError=validateMaterial(file),safeTitle=cleanText(title,160);if(fileError||!isUuid(courseId)||safeTitle.length<3){setMessage(fileError||'تحقق من المقرر والعنوان.');return;}setBusy(true);const db=await getSupabase(),path=`${courseId}/${crypto.randomUUID()}`;const {error:uploadError}=await db.storage.from('course-materials').upload(path,file,{cacheControl:'3600',upsert:false,contentType:file.type});if(uploadError){setMessage('فشل رفع الملف.');setBusy(false);return;}const {error}=await db.from('materials').insert({course_id:courseId,title:safeTitle,file_path:path,file_name:cleanText(file.name,180),mime_type:file.type,size_bytes:file.size,uploaded_by:profile.id,status:'published'});if(error){await db.storage.from('course-materials').remove([path]);setMessage('فشل حفظ بيانات الملف.');}else{setMessage('تم رفع المادة ونشرها بنجاح.');setFile(null);setTitle('');}setBusy(false);}return <div className="contentBlock narrow"><div className="blockHead"><div><h1>رفع مادة علمية</h1><p>الحد الأقصى 25MB للملف.</p></div></div><form className="uploadForm" onSubmit={submit}><label>معرّف المقرر<input value={courseId} onChange={e=>setCourseId(e.target.value)} maxLength="36" required/></label><label>عنوان المادة<input value={title} onChange={e=>setTitle(e.target.value)} maxLength="160" required/></label><label className="drop"><Icon name="upload"/><strong>{file?file.name:'اختر ملفًا للرفع'}</strong><span>PDF أو PowerPoint أو Word</span><input type="file" accept=".pdf,.ppt,.pptx,.doc,.docx" onChange={e=>setFile(e.target.files[0]||null)} required/></label>{message&&<p role="status">{message}</p>}<button disabled={busy}>{busy?'جاري الرفع…':'رفع ونشر'}</button></form></div>}
