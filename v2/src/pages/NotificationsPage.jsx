import {useEffect,useState} from 'react';import {getSupabase} from '../lib/supabase';
export function Notifications(){const [rows,setRows]=useState([]),[error,setError]=useState('');async function load(){const db=await getSupabase();const {data,error:e}=await db.from('notifications').select('id,title,body,link,read_at,created_at').order('created_at',{ascending:false}).limit(100);if(e)setError(e.message);setRows(data||[])}useEffect(()=>{load()},[]);async function read(row){if(row.read_at)return;const db=await getSupabase();const {error:e}=await db.from('notifications').update({read_at:new Date().toISOString()}).eq('id',row.id);if(e)setError(e.message);else load()}return <div className="ops"><div className="pageTitle"><div><span>آخر التحديثات الأكاديمية</span><h1>الإشعارات</h1></div></div>{error&&<div className="status">{error}</div>}<section className="cards">{rows.length?rows.map(row=><article className="record" key={row.id} onClick={()=>read(row)} style={{borderRight:row.read_at?'1px solid var(--line)':'4px solid var(--brand)',cursor:'pointer'}}><div className="recordHead"><div><h3>{row.title}</h3><small>{new Date(row.created_at).toLocaleString('ar-EG')}</small></div>{!row.read_at&&<span className="badge">جديد</span>}</div><p>{row.body}</p></article>):<p className="muted">لا توجد إشعارات.</p>}</section></div>}
