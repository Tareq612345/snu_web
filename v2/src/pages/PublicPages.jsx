import {BookOpen,Building2,ShieldCheck,Upload} from 'lucide-react';import {Navigate,useNavigate} from 'react-router-dom';import Brand from '../components/Brand';import {useAuth} from '../context/AuthContext';import {isSupabaseConfigured,supabase} from '../lib/supabase';import {useState} from 'react';
export function Landing(){const nav=useNavigate();return <div className="landing"><header><Brand/><button onClick={()=>nav('/login')}>تسجيل الدخول</button></header><main><div className="heroCopy"><span className="eyebrow">منصة المواد والمقررات الجامعية</span><h1>تعليم جامعي منظم، في بوابة واحدة.</h1><p>وصول واضح إلى الكتب والمذكرات والمحاضرات والواجبات، مع بوابة مستقلة لأعضاء هيئة التدريس.</p><button onClick={()=>nav('/login')}>الدخول إلى البوابة</button></div><div className="bookPanel"><BookOpen size={46}/><strong>المكتبة الرقمية</strong><span>المقررات • الكتب • المحاضرات • الملفات</span></div></main><section className="features"><article><Building2/><h3>هيكل جامعي واضح</h3><p>الكليات والأقسام والمقررات في تسلسل بسيط.</p></article><article><BookOpen/><h3>محتوى أكاديمي</h3><p>كتب وملفات ومحاضرات منظمة لكل مقرر.</p></article><article><Upload/><h3>بوابة للدكاترة</h3><p>رفع المواد وإدارتها ومتابعة الطلاب.</p></article><article><ShieldCheck/><h3>صلاحيات مؤسسية</h3><p>حماية البيانات بسياسات Supabase RLS.</p></article></section></div>}
export function Login(){const {session,profile}=useAuth();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);if(session&&profile)return <Navigate to={profile.role==='faculty'?'/faculty':'/student'} replace/>;async function submit(e){e.preventDefault();if(!isSupabaseConfigured){setError('يجب إضافة متغيرات Supabase أولاً.');return;}setBusy(true);setError('');const {error:err}=await supabase.auth.signInWithPassword({email,password});if(err)setError('تعذر تسجيل الدخول. تحقق من البريد وكلمة المرور.');setBusy(false);}return <div className="loginPage"><div className="loginCard"><Brand/><h1>تسجيل الدخول</h1><p>استخدم حسابك الجامعي للوصول إلى البوابة المناسبة.</p><form onSubmit={submit}><label>البريد الجامعي<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>كلمة المرور<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="error">{error}</div>}<button disabled={busy}>{busy?'جاري الدخول…':'دخول'}</button></form><a href="/">العودة للرئيسية</a></div></div>}
